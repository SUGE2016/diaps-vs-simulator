/**
 * 画布相关常量
 */
export const ELEMENT_SIZES = {
  WORKSTATION: { width: 80, height: 60 },
  BUFFER: { width: 70, height: 50 },
};

export const ALIGNMENT_CONFIG = {
  SNAP_THRESHOLD: 5, // 对齐吸附阈值（像素）
  ALIGNMENT_GUIDE_THRESHOLD: 10, // 显示对齐参考线的阈值（像素）
  MIN_SPACING: 10, // 最小间距（像素）
};

export const CANVAS_CONFIG = {
  MIN_WIDTH: 600,
  MIN_HEIGHT: 400,
  DEFAULT_HEIGHT: 600,
  PADDING: 20, // 内容边距
};

export const CONNECTION_HANDLE_SIZE = 12;
export const ARROW_OFFSET = 6;
export const LINE_CLICK_AREA_WIDTH = 10;

